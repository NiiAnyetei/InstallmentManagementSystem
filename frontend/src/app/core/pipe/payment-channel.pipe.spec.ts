import { PaymentChannelPipe } from './payment-channel.pipe';

describe('PaymentChannelPipe', () => {
  it('create an instance', () => {
    const pipe = new PaymentChannelPipe();
    expect(pipe).toBeTruthy();
  });
});
