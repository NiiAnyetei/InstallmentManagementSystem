import { CyclePeriodPipe } from './cycle-period.pipe';

describe('CyclePeriodPipe', () => {
  it('create an instance', () => {
    const pipe = new CyclePeriodPipe();
    expect(pipe).toBeTruthy();
  });
});
